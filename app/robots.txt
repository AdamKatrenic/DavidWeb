import { MetadataRoute } from 'next'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: ['/admin', '/studio'], // Zakážeme indexovať Sanity Studio
    },
    sitemap: 'https://davidpillar.sk/sitemap.xml',
  }
}